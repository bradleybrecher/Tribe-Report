function isValidURI(){
	// check through the backend if form posted
	// if not
		// make that field red through DOM

}

function isFilter(){
	// what to do if filter goes through
	// if submit, change the name to revert and vice versa
}


// get a timestamp to put on time ??

// implement login

// (6 points) minimum 3 x any of the following (can be the same):
// es6 classes that you've written yourself (using the class keyword)
// Object.create (where prototype matters)
// original higher order functions that you've written yourself
// or use any of these built-in higher order functions found in Array.prototype: map, reduce, filter


// data specified as private to a user cannot be viewed by another user --> does this mean on frontend